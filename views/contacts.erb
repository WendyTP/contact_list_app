<p>You have <%= count_contacts %> contacts:</p>
<% @contacts.each do |contact| %>
  <ul>
    <li>
      <a href=/contacts/<%= contact[:id] %>><%= contact[:firstname]%> <%= contact[:lastname]%></a>
    </li>
  </ul>
<% end %>

<a href="/">Back</a>